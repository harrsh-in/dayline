# Claude Frontend Instructions

Use root `CLAUDE.md` and `admin-panel/AGENTS.md`.

When editing the frontend:

- Inspect nearby route/component/API files first.
- Keep UI minimal and calm.
- Use semantic component names.
- Use typed API functions.
- Keep provider-specific details out of JSX.
- Preserve existing Tailwind 4 token usage.
- Run relevant checks before finalizing when possible.
